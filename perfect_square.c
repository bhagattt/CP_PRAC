#include <stdio.h>
#include <math.h>
int main()
{
    int a,root;
    double root1;

printf("Enter the number \n");
scanf("%d",&a);
root=sqrt(a);
root1=sqrt(a);

if(root1==root)
printf("YES");
else 
printf("NOOO");
}