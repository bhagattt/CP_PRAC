#include <stdio.h>
#include <math.h>
int main()
{
    int root,n;
    double root1;
printf("Enter the number \n");
scanf("%d",&n);
root1=sqrt(n);
root=sqrt(n);

if(root1==root)
printf("%d is a perfect s",n);
else
printf("IT aint");
}